# secrets.properties.example — copy to secrets.properties and fill in real values.
# secrets.properties is gitignored and must NEVER be committed.
#
# CI: set these as environment variables with the same names;
#     the convention plugin reads env vars as the fallback when the file is absent.

MQTT_BROKER_URI=ssl://REPLACE_WITH_IOT_ENDPOINT.iot.us-east-1.amazonaws.com:8883
VIGIA_API_BASE_URL=http://REPLACE_WITH_ALB_ENDPOINT
STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
BLACKBOX_MAC=00:00:00:00:00:00
