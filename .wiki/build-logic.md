# build-logic

The `:build-logic` composite build contains Gradle convention plugins.

## Connects To

- Used by all Android modules through plugin management.

## Role

- Centralizes build configuration for the repo.
- Keeps Android, Compose, Hilt, and feature conventions consistent.